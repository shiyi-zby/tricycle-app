name: Build Kivy APK
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build_apk:
    runs-on: ubuntu-22.04
    steps:
      - name: 检出项目代码
        uses: actions/checkout@v5

      - name: 配置Python3.10
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"

      - name: 替换清华Ubuntu源
        run: |
          sudo cp /etc/apt/sources.list /etc/apt/sources.list.bak
          sudo tee /etc/apt/sources.list <<'EOF'
          deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy main restricted universe multiverse
          deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse
          deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-backports main restricted universe multiverse
          deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-security main restricted universe multiverse
          EOF
          sudo apt update

      - name: 安装编译依赖
        run: |
          sudo apt install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev automake
          pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
          pip install buildozer

      - name: Git全局镜像加速
        run: |
          git config --global url."https://gitee.com/mirrors/".insteadOf "https://github.com/"
          git config --global url."https://gitee.com/mirrors/".insteadOf "git@github.com:"
          git config --global http.postBuffer 524288000

      - name: 预克隆python-for-android避免目录缺失
        run: |
          mkdir -p .buildozer/android/platform
          git clone --depth 1 https://gitee.com/mirrors/python-for-android.git .buildozer/android/platform/python-for-android

      - name: 清理缓存并自动同意SDK协议打包
        run: |
          buildozer android clean
          yes | buildozer -v android debug

      - name: 上传打包产物APK
        uses: actions/upload-artifact@v4
        with:
          name: 打包好的安卓APK
          path: bin/*.apk
